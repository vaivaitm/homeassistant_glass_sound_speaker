set_sound_setting:
  target:
    entity:
      integration: lspx_s2
      domain: media_player
  fields:
    name:
      required: true
      example: "nightMode"
      selector:
        text:
    value:
      required: true
      example: "on"
      selector:
        text:
