set_sound_setting:
  target:
    device:
      integration: lspx_s2
  fields:
    name:
      required: true
      example: "nightMode"
      selector:
        text:
    value:
      required: true
      example: "on"
      selector:
        text:
